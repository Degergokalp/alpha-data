# Supabase project holding the `reports` bucket and reddit_alpha_* tables.
ALPHA_SUPABASE_URL=https://bvggujwxalckfrrzjbwg.supabase.co
# Anon (publishable) key — public reads only.
ALPHA_SUPABASE_ANON_KEY=

# Used only in stdio MCP mode (tools call the hosted REST API).
ALPHA_API_URL=https://alpha-data.onrender.com

# Cache and archive walk-back tuning.
ALPHA_CACHE_TTL_SEC=300
ALPHA_MAX_LOOKBACK_DAYS=14
